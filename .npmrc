# npm publishing configuration for archifind

# Ensure we're publishing to the official npm registry
registry=https://registry.npmjs.org/

# Use exact versions (no tilde or caret ranges)
save-exact=true

# Require confirmation for risky operations
engine-strict=true
legacy-peer-deps=false
